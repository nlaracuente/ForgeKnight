using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Handles the stats changes as units increase in level
/// As well as creating what the stats increase will be to display it
/// </summary>
public class StatsManager : MonoBehaviour
{
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
